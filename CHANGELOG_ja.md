# Changelog

### 2026.03.05
 - ファーストリリース(綴じ方向設定、表紙設定、見開き表示、画像保存、二重ZIP対応、PDFアノテーションコメント対応)
 - 左右タップエリアでページ送り
 - ファイル名文字化け修正機能
 - ホイールでページ送り

### 2026.03.09
 - 中央部クリックでヘッダパネルを隠す
 - スワイプでページ送り

### 2026.03.10
 - 縮小表示高画質化

### 2026.03.12
 - 見開き表示時の保存ボタン分割

### 2026.03.14
 - ePub対応（前からZIPとして開けてはいた）
 - 拡大表示時パンモード
 - 拡大表示時ミニマップ表示

### 2026.03.15
 - しおり機能
 - 回転表示
 - GIFアニメ再生
 - WebP/PNGアニメ再生
 - libarchivejsの脆弱性対策
 - HEIC対応(Safariのみ)

### 2026.03.16
 - パスワード付き書庫への対応
 - パスワード付きPDFへの対応
 - UIにツールチップ表示
 - フルスクリーンモード(※iOSでは機能しない)
 - テキスト検索/選択 (pdf-viewerのみ)
 - 画像をクリップボードにコピー

### 2026.03.17
 - 縦スクロール表示モード
 - 色調補正フィルタ
 - ショートカットキー割り当て追加（綴じ方向、表紙有無、既読スキップ）
 - ヘッダパネルを3行→2行に
 - ウィンドウ幅が狭くてヘッダパネルが折り返している時のFitを修正
 - ズーム機能(ショートカットキーのみ)
 - 既読スキップ確認表示
 - PDFアノテーションコメント表示ができなくなっていたのを修正
 - 画像リサイズ処理をPica.jsで置き換え

### 2026.03.18
 - 画像リサイズ処理にWASM-VIPSを利用できるようにする(?vips=1)

### 2026.04.17
 - PWA対応（オフラインモード）

### 2026.05.11
 - ギャップレスモード追加 (スクロール表示時のみ)

### 2026.05.13
 - iOSでフォントが埋め込まれていないPDFの日本語(CJK)文字が表示されない問題に対応（代替フォント追加）
 - タイトルクリックでリセット

### 2026.06.27
 - 読み込み中に「応答なし」確認ダイアログが出るのを抑制

### 2026.06.29
 - PWAでメモリ不足になることがあるのを対策
 - 特定の解像度でスクロールバーがガタつくのを対策
 - ページめくり時のチラつき防止

### 2026.07.30
 - wasm-vips を v0.0.16 (libvips 8.17.3) から v0.0.18 (libvips 8.18.3) に更新
   - libvips の脆弱性 CVE-2026-33327 / 33328 / 35590 / 35591 を修正済みのバージョンに追随
   - 副次効果: WASM バイナリが約9%小型化 (5.4MB → 4.8MB)、大きな画像の処理速度改善
 - Pica.js を v9.0.1 から v10.0.2 に更新
   - Chrome の createImageBitmap の Exif orientation バグ回避、Safari の判定修正
   - canvas context を willReadFrequently 付きで生成するようになり、縮小処理が高速化
 - 縮小処理を Web Worker に移動 (メインスレッドのブロック解消)
   - 12ページのアーカイブ読み込みで、所要時間が 2.5秒 → 1.1秒、UIが固まる最長時間が 316ms → 66ms に短縮
   - 読み込み中もページ送りやスクロールが引っかからなくなる
   - Chrome でタイル境界に筋が入る Pica の不具合 (nodeca/pica#223 の再発) を回避済み。画質は Worker 化前と完全に同一

### 2026.08.06
 - EPUB 構造解析機能 (comic-viewer.html)
   - `E` キーで EPUB の内部構造 (container.xml → OPF) を解析し、正しいページ順に並べ替え
     - OPF の spine の順に XHTML を辿り、各ページの `<img>` / `<svg><image>` / 背景画像を文書順に収集
     - spine から画像が取れない EPUB は manifest の `media-type="image/*"` の記述順にフォールバック
     - 解析結果は並び順プルダウンに `Sort: EPUB` として追加され、他の並び順といつでも切替可能
     - 読み順に現れない画像 (未参照のカバー等) は末尾に Natural 順で残るので、ページが消えることはない
   - 重い処理なので自動実行はせず、`E` キーを押したときだけ実行する
 - EPUB 目次 (TOC) 表示 (comic-viewer.html)
   - 構造解析で目次が見つかるとサイドバーに `TOC` タブが出現。`T` キーで開閉
   - EPUB3 の nav ドキュメントを優先し、無ければ EPUB2 の NCX を使用。階層はインデントで表現
   - 各項目に対応ページ番号を表示し、クリックでジャンプ。現在位置の項目をハイライト
   - 見開き表示では「表示中の最大ページ」で現在位置を判定 (右ページ始まりの章を選んだときに1つ前の項目が光るのを防止)
 - 並び順を変更したとき、Scroll モードの表示が古い順序のまま残ることがあるのを修正

### 2026.08.08
 - リフロー型 EPUB の本文テキスト表示 (comic-viewer.html)
   - `E` キーの構造解析後、`R` キーまたは `TOC` タブ下部の「本文を読む」で本文リーダーを起動
     - spine の読み順に本文文書を並べ、目次のラベルを付けて一覧表示。前後移動・プルダウン選択に対応
     - 文字サイズ変更 (A- / A+)、「原文CSS」トグル (EPUB 本体のスタイルシートを適用するか)
     - 対応ページがある文書は `→ Page` ボタンでページ表示へジャンプ
   - 画像を1枚も含まないリフロー型 EPUB も開けるようになった (案内用のプレースホルダページを1枚生成し、そのまま構造解析 → 本文リーダーを自動起動)
   - 縦書き指定は横書きに矯正 (本ビューアは縦書き要件を対象外としている)
 - 本文リーダーは外部リソースへ一切アクセスしない
   - `allow-scripts` を与えない sandbox iframe で表示するため、EPUB 内の JavaScript は実行されない
   - 注入する CSP が `default-src 'none'` なので、書き換え漏れがあってもネットワークに出ない
   - 書き換え時に http(s):// や protocol-relative の URL を除去し、`script` / `iframe` / `object` / `embed` / `audio` / `video` / `form` 要素と `on*` 属性も削除
   - 画像・CSS はアーカイブ内から取り出して blob: URL に差し替えて表示 (ローカル画像・背景画像は正しく表示される)
   - リンクは同一文書内のフラグメント (`#...`) のみ有効、他文書・外部リンクは無効化

### 2026.08.08 (2)
 - EPUB 本文リーダーに表示幅の切替を追加 (`幅: 標準` ⇄ `幅: 広い`)
   - 標準: 従来どおりモーダル最大 820px / 本文の折り返し 44em
   - 広い: モーダル最大 1600px / 本文の折り返し 78em
   - 選択状態は localStorage (`viewerEpubReaderWide`) に保存され、次回以降も維持される

### 2026.08.11
 - アクセス制限のあるネットワーク共有フォルダ等からファイルを開いた時に失敗して固まることがあるのを対策（それでも開けない時はエラーを表示）

### 2026.08.14
 - サーバー設置時のライブラリ参照機能を追加 (`library.php`)
   - サーバー上の指定フォルダを一覧・検索して直接開けるようになった。ヘッダーの `Library` ボタン / `O` キー / ドロップ画面のボタンから起動
   - フォルダ移動 (パンくず)、名前 (自然順) / 更新日時 / サイズでの並び替え、インクリメンタルサーチ、サブフォルダ横断検索に対応
   - しおりが有効なら一覧に既読ページのバッジが出る。ローカルで開いた同じファイルのしおりとそのまま共有される
   - `?lib=サブフォルダ/ファイル名.cbz` で起動時に自動で開ける
   - 転送が途中で切れた場合は Range リクエストで続きから再開する
   - 参照できるのは設定したフォルダ以下のみ (下位フォルダには移動可能)。設定フォルダは公開フォルダの外に置けるので、直リンクでのダウンロードを不可能にできる
   - Basic 認証は `library.config.php` の `'auth'` に ID / パスワードを書くだけで掛けられる (サーバー設定不要、パスワードは `password_hash()` のハッシュも可)。`.htaccess` 等で掛ける場合は `library.php` にだけ掛けること (アプリ全体に掛けると Service Worker のプリキャッシュが失敗しオフライン動作が壊れる)
 - ローカルファイルを開く従来の機能はそのまま。`library.php` が無いサーバー / GitHub Pages 版では Library の UI は自動的に隠れる

### 2026.08.14 (2)
 - ライブラリ参照に表紙画像のプレビューとサムネイル表示を追加
   - 表紙は**事前に用意された画像ファイルがあるものだけ**表示 (自動生成はしない)。`vol01.cbz` に対して `vol01.cbz.coverimage.webp` のように、元のファイル名の後ろに `.coverimage.<拡張子>` を付けた画像を隣に置く
     - 元のファイル名を丸ごと残すので `vol01.cbz` と `vol01.pdf` が同居しても衝突しない
     - 対応形式は webp / avif / png / jpg / jpeg / gif。命名規則は `coverSuffix` / `coverExts` で変更可
   - マウスオーバー (0.3秒) またはロングタップ (0.45秒) で表紙を拡大プレビュー
   - ツールバーで「リスト表示 ⇄ サムネイル表示」を切替 (選択状態は次回も維持)
   - サムネイル用の縮小画像を別途用意する必要はない。サーバーに GD があれば表示サイズに合わせて自動縮小し、ブラウザにキャッシュさせる (表紙のみキャッシュ許可、本体は従来どおり no-store)
   - 表紙画像そのものは一覧にも出ず、直接ダウンロードもできない (本のパスから表紙名をサーバー側で導出する方式のため)
